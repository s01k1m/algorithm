def bfs(s):
    # [0] q, v, 필요한 flag 생성
    q = []
    v = [0] * 101

    # [1] q에 초기데이터(들) 삽입, v표시

    while q:
        # [2] q에서 한개 꺼냄 + 필요시 정답 처리
        pass

        # [3] 4/ 8 연결방향 등 반복처리
        for n in adj[c]:
            if v[n] == 0:
                pass


return ans

T = 10
for test_case in range(1, T + 1):
    N, S = map(int, input().split())

    # [1] 인접리스트에 연결 저장
